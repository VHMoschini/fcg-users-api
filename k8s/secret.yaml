apiVersion: v1
kind: Secret
metadata:
  name: users-api-secret
type: Opaque
stringData:
  jwt-key: "CHANGE_ME_DEV_KEY_MIN_32_CHARS_LONG!!"
  connection-string: "Data Source=/app/data/users.db"
  rabbitmq-username: "guest"
  rabbitmq-password: "guest"
